---
version:  1.1
kind: operation
params:
  loss: MeanSquaredError
  flag: true
component:
  tags: [foo, bar]
  inputs:
    - name: loss
      type: str
    - name: flag
      type: bool
      isFlag: true
  run:
    kind: job
    container:
      image: my_image
      command: ["/bin/sh", "-c"]
      args: video_prediction_train --loss={{loss}} {{ flag }}
