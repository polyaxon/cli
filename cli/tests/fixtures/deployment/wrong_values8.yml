deploymentType: docker-compose

rbac:
  enabled: true

gateway:
  service:
    type: ClusterIP

environment: staging

user:
  password: root

postgresql:
  enabled: true

redis:
  enabled: true

rabbitmq:
  enabled: true

scheduler:
  enabled: true
